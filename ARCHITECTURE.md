# 多用户 Agent 数据隔离架构

## 1. 目标

本系统允许多个独立用户通过 Agent 执行数据抓取、清洗、分析和问答。每个用户可维护个人偏好、兴趣、第三方 API Key、社交账号连接和长期记忆。

核心安全目标是：**任一用户、其 Agent、任务、凭据、数据与记忆均属于独立租户；用户 A 不得读取、修改、删除、推断或在上下文中间接获得用户 B 的任何私有信息。**

本文以单人个人空间为基本租户（`tenant_id = user_id`）。后续如需团队空间，可将租户抽象为 `workspace_id`，并在工作区内另行定义成员授权；不得以共享数据库表但无租户约束的方式实现。

## 2. 范围与非目标

### 2.1 本期范围

- 账号身份认证、会话管理和租户上下文传递。
- 用户偏好、第三方凭据、已授权社交账号的安全存储与使用。
- 抓取任务、原始抓取数据、处理数据、分析结果和导出文件的隔离。
- Agent 的短期上下文、长期记忆、检索索引和工具调用的隔离。
- 管理、日志、备份、缓存和异步队列中的隔离与审计。

### 2.2 非目标

- 绕过第三方平台的授权、反爬、访问控制或服务条款。
- 让 Agent 代替用户保管不可恢复的主密钥或助记词。
- 在未设计细粒度授权、审计和撤销机制前提供跨用户共享、公开数据集或共享 Agent 记忆。
- 将“提示词中要求不要泄露”视为安全隔离手段。

## 3. 术语与数据分类

| 术语 | 定义 | 示例 |
| --- | --- | --- |
| 租户 | 数据与授权的最小隔离单位 | 一个个人用户空间 |
| 主体 | 发起访问的身份 | 用户、后台服务、管理员 |
| 租户上下文 | 全链路携带的 `tenant_id`、主体和权限 | JWT Claims、任务载荷 |
| Secret | 一旦泄露即导致外部系统被访问的数据 | API Key、OAuth refresh token、Cookie |
| 私有数据 | 仅所属租户可访问的数据 | 抓取结果、偏好、分析报告 |
| Agent 记忆 | 供模型后续检索或个性化使用的信息 | 偏好摘要、历史结论、向量索引 |

数据按敏感度分为：

1. `SECRET`：API Key、OAuth token、会话 Cookie。只可由密钥服务解密后在受控执行环境中短暂使用。
2. `PRIVATE`：原始数据、分析产物、聊天内容、偏好、记忆。默认仅租户所有者可读写。
3. `INTERNAL`：脱敏后的运行指标与错误码。不得包含私有内容或凭据。
4. `PUBLIC`：用户明确发布且经过发布流程的数据。不得由默认私有流程产生。

## 4. 架构

```text
客户端
  |  OIDC / Session
  v
API 网关 ----> 身份与授权服务
  |                 |
  |  已验证 tenant_id + subject + scopes
  v                 v
应用 API ------> 配置服务 ------> 密钥管理服务 (KMS / Vault)
  |                    |                    |
  |                    |                    +--> 短期凭据注入
  |                    v
  +--> 任务编排器 --> 租户队列 --> 隔离执行 Worker --> 第三方 API / 网站
  |                      |                 |
  |                      v                 v
  |                 任务元数据          原始数据对象存储
  |                                          |
  +--> Agent 编排服务 --> 租户记忆检索 ------+
  |          |                |
  |          |                +--> 元数据数据库 / 向量库
  |          v
  +--> 分析服务 --> 结果数据库 / 对象存储

所有私有数据面：数据库 RLS + 对象存储按租户前缀授权 + 服务层租户校验。
```

### 4.1 控制面与数据面

- **控制面**：身份、授权、任务定义、连接配置、审计策略。控制面决定“谁能发起什么操作”。
- **数据面**：抓取、存储、分析、检索、导出。数据面仅接受经过验证的租户上下文，且不得从请求体、模型输出或任务参数自行信任 `tenant_id`。
- **密钥面**：密钥管理服务独立部署。应用数据库只能保存密钥引用、版本和最少的显示元数据，不能保存明文 Secret。

### 4.2 推荐逻辑实体

每张含私有数据的表必须有非空 `tenant_id`，并建立以它为前缀的索引。

| 实体 | 必要字段 | 隔离要求 |
| --- | --- | --- |
| `users` | `id`, `identity_provider_subject` | 身份映射，不存业务私密数据 |
| `tenant_settings` | `tenant_id`, `preferences` | 一租户一份或版本化配置 |
| `connections` | `id`, `tenant_id`, `provider`, `secret_ref`, `status` | 仅存 Secret 引用，不存 token 明文 |
| `jobs` | `id`, `tenant_id`, `connection_id`, `state` | `connection_id` 必须属于同一租户 |
| `datasets` | `id`, `tenant_id`, `object_key`, `classification` | `object_key` 固定在租户前缀内 |
| `analysis_runs` | `id`, `tenant_id`, `input_dataset_id`, `result_ref` | 输入与输出必须同租户 |
| `memory_items` | `id`, `tenant_id`, `scope`, `content_ref`, `embedding_ref` | 检索时强制租户过滤 |
| `audit_events` | `id`, `tenant_id`, `actor_id`, `action`, `resource_id` | 可审计，不记录原文 Secret |

## 5. 隔离设计

### 5.1 身份、会话与授权

1. 身份提供方完成登录后，授权服务签发包含不可伪造 `sub`、`tenant_id`、角色和有效期的访问令牌。
2. API 网关验证令牌并从 Claims 构造服务端租户上下文；客户端提交的 `tenant_id` 仅可作为显示或筛选提示，不能作为授权依据。
3. 服务间调用使用工作负载身份或短期服务令牌，并显式传递经签名的租户上下文。
4. 所有资源 ID 查询都同时按 `id` 和 `tenant_id` 限定。仅按全局 `id` 查询后再在业务代码中判断归属是不足够的。
5. 管理员默认没有原始数据读取权限。紧急访问需双人审批、限定资源与时长，并留下不可篡改审计记录。

### 5.2 数据库隔离

首选共享数据库加行级安全（Row-Level Security, RLS），并将其视为第二道强制边界：

```sql
ALTER TABLE datasets ENABLE ROW LEVEL SECURITY;

CREATE POLICY tenant_isolation ON datasets
USING (tenant_id = current_setting('app.tenant_id', true)::uuid)
WITH CHECK (tenant_id = current_setting('app.tenant_id', true)::uuid);
```

- 每个事务开始时，由可信数据库访问层设置 `SET LOCAL app.tenant_id = ...`；连接归还池前必须清理事务状态。
- 应用运行账号不得拥有 `BYPASSRLS`，迁移账号与运行账号分离。
- 外键必须防止跨租户关联。可使用复合唯一键 `(tenant_id, id)` 与复合外键，或在数据库触发器中强制校验。
- 分区、备份和分析查询仍必须保留 `tenant_id`。不能因性能优化建立绕过 RLS 的共享物化视图。

高合规或高价值客户可升级为独立数据库或独立加密密钥；应用接口不变，但这是更高成本的隔离等级，而不是共享库 RLS 的替代借口。

### 5.3 对象存储、导出与缓存

- 对象键格式固定为 `tenants/{tenant_id}/{resource_type}/{resource_id}/...`，服务端生成，不接受客户端任意路径。
- Worker 获得仅可读写当前任务目录的短期预签名 URL 或临时角色；不得授予整个 bucket 权限。
- 下载链接必须短期有效、绑定具体对象，并在生成前再次授权。不得暴露可枚举的永久公网 URL。
- 缓存键、消息主题、临时文件名、搜索索引名称都必须包含 `tenant_id`。例如 `analysis:{tenant_id}:{run_id}`。
- 删除或撤销授权时，清除导出链接、缓存、临时文件及派生索引；异步清理由带租户约束的任务执行。

### 5.4 凭据与社交账号连接

- 优先采用 OAuth 授权码流程和最小 scope；API Key 仅在必要时支持。
- Secret 只写入 KMS/Vault，数据库保存 `secret_ref`、提供方、创建时间、过期时间和最后四位等非敏感元数据。
- 解密操作只能由持有对应 `tenant_id`、连接 ID 和任务用途的 Worker 申请；返回值仅在内存中存在，禁止写日志、消息队列、错误详情或 Agent 上下文。
- 一个连接只能被同租户的任务引用。任务创建时验证 `jobs.tenant_id = connections.tenant_id`。
- 支持轮换、失效、撤销和“断开连接”；断开后立即停止新任务，吊销或删除 Secret，并根据保留策略清理关联数据。

### 5.5 抓取与分析执行隔离

- 任务进入队列时固定写入服务端生成的 `tenant_id`、允许的数据集 ID 和最小工具权限；Worker 不得接受模型生成的资源 ID 来扩大读取范围。
- 每个 Worker 在容器或沙箱中执行，使用只读基础镜像、非 root 用户、网络出口白名单、资源配额和任务超时。
- 临时工作目录按任务和租户创建，任务结束后安全清理。不同任务不能复用浏览器 profile、Cookie jar 或本地缓存。
- 抓取工具只能访问显式允许的站点及当前任务的已授权连接；不要把用户提供的任意 URL 直接交给具备内网访问能力的 Worker，以防 SSRF。
- 分析程序以数据集白名单读取输入，输出自动标记同一 `tenant_id`。禁止跨租户聚合，除非进入单独的、明确同意且已匿名化的产品流程。

### 5.6 Agent 记忆与模型上下文隔离

Agent 的记忆分为三层，均须带 `tenant_id`：

| 层级 | 生命周期 | 允许内容 | 隔离方式 |
| --- | --- | --- | --- |
| 会话上下文 | 单次请求或会话 | 当前任务输入、短期状态 | 每次请求新建；不在不同用户间复用对话历史 |
| 工作记忆 | 单任务 | 中间摘要、工具结果引用 | 绑定 `job_id` 和 `tenant_id`；任务结束即删除或归档 |
| 长期记忆 | 可配置保留期 | 偏好、确认过的结论、用户允许保存的摘要 | 单独存储，检索与写入均强制租户过滤 |

具体规则：

1. 向量库的每个记录写入 `tenant_id` 元数据，所有相似度检索使用不可省略的服务端过滤条件。不要依赖“向量距离足够远”实现隔离。
2. 记忆写入经过策略层：拒绝 Secret、访问 token、完整 Cookie、身份证件、支付信息等高风险内容；对允许保存的信息记录来源、用途、保留期和用户撤销状态。
3. Agent 工具层根据租户上下文生成可访问资源清单。模型只能看到已获授权的数据引用与必要内容，不能调用“按任意 ID 读取数据”的通用工具。
4. 共享模型提供商调用前，移除凭据，最小化发送字段，并确认供应商的数据保留与训练政策满足产品承诺。不同租户的提示和响应不应通过本地提示缓存、追踪系统或评测集相互可见。
5. 用户可查看、修改、删除和禁止继续生成其长期记忆；删除请求同时删除向量、摘要、关联原文和缓存，备份按既定过期周期清除。

### 5.7 日志、监控、备份与支持

- 结构化日志使用 `tenant_id`、`request_id`、`job_id` 等可审计标识，但禁止记录请求正文、抓取正文、Authorization 头、Cookie、API Key、OAuth token 和完整个人资料。
- 错误采集系统应用字段白名单与脱敏器；任何异常栈附带的上下文也需扫描 Secret 模式。
- 指标仅汇总计数、耗时、失败类型等内部数据。若需按租户排障，访问需受 RBAC 与审计约束。
- 备份加密，访问最小化，恢复演练须在隔离环境进行。租户删除 SLA、备份保留期与法律保留例外需要公开说明。
- 客服排障优先使用脱敏诊断包；读取私有原文属于受控支持访问，必须有用户授权或紧急流程记录。

## 6. 功能与非功能需求

### 6.1 必须满足的功能需求

| 编号 | 需求 | 验收标准 |
| --- | --- | --- |
| FR-01 | 用户只能查看和操作自己的配置、连接、任务、数据集、结果与记忆 | 将 A 的任意资源 ID 传给 B 的 API，返回 `404` 或统一的未授权结果，且无资源存在性泄露 |
| FR-02 | 用户可添加、轮换、撤销第三方连接 | Secret 不出现在数据库明文、API 响应、日志和 Agent 提示中 |
| FR-03 | 任务与数据集始终具有不可为空的租户归属 | 任意创建、导入、复制和异步处理都无法产生无 `tenant_id` 的私有记录 |
| FR-04 | Agent 仅检索和使用当前租户的上下文与记忆 | 跨租户“探针”测试中，检索结果和模型输入不含对方记录 |
| FR-05 | 用户可导出和删除自己的数据及记忆 | 导出仅包含本人数据；删除覆盖主存储和派生存储，并进入备份过期流程 |
| FR-06 | 安全事件可追溯 | 私有数据、Secret 使用、导出、管理员紧急访问均有审计事件 |

### 6.2 非功能与安全需求

- **默认拒绝**：未建立租户上下文、上下文不匹配或授权不完整时拒绝访问。
- **最小权限**：服务、Worker、数据库账号、对象存储角色和第三方 scope 都只获得完成当前操作的权限。
- **纵深防御**：应用授权、数据库 RLS、对象存储 IAM、队列授权和检索过滤同时存在；任何单层失败不应直接导致跨租户泄露。
- **加密**：传输使用 TLS；静态数据、备份和 Secret 使用受管理密钥加密。高风险租户支持每租户密钥。
- **可用性**：一个租户的超大抓取或失败重试不可耗尽其他租户的队列配额、并发或存储预算。
- **可观察性**：每次操作可按 `request_id` 与 `job_id` 追踪，同时日志不泄露私有内容。
- **合规**：明确隐私政策、数据用途、保留期、导出和删除路径；按目标市场补充适用的个人信息与跨境传输义务。

## 7. 信任边界与禁止事项

| 边界 | 不可信输入 | 强制控制 |
| --- | --- | --- |
| 客户端 -> API | 资源 ID、租户 ID、筛选条件、上传文件 | 服务端身份验证、对象归属校验、内容与大小限制 |
| API -> 任务队列 | 任务参数、重试消息 | 签名任务载荷、租户固定、白名单资源、幂等键 |
| Worker -> 外部网络 | URL、网页内容、第三方响应 | 出口控制、SSRF 防护、内容隔离、超时与配额 |
| Agent -> 工具 | 模型生成的工具参数、提示注入内容 | 服务端资源白名单、参数校验、最小工具能力 |
| 应用 -> 数据/记忆 | 查询条件、向量检索条件 | RLS、固定 `tenant_id` 过滤、禁止裸查询 |
| 运维 -> 生产数据 | 排障请求、管理员身份 | JIT 权限、审批、审计、脱敏优先 |

以下行为明确禁止：

- 在前端保存、打印或回传完整 API Key、refresh token 或 Cookie。
- 使用全局共享的浏览器 profile、临时目录、Redis key、向量 collection 或 Agent 对话历史而不加租户命名空间。
- 通过系统提示词、模型“自律”或 UI 隐藏来代替服务端授权。
- 让异步 Worker 根据用户输入或模型输出自由切换 `tenant_id`、数据集 ID 或密钥引用。
- 将生产私有数据直接放入开发、测试、评测、观测或模型调试系统。
- 在没有显式、可撤销同意和匿名化评审的情况下，把某用户数据用于其他用户的推荐、训练或分析。

## 8. 关键流程

### 8.1 创建抓取任务

1. 用户完成认证，API 网关验证令牌并构造租户上下文。
2. 应用读取当前租户的 `connection_id`，以 `(tenant_id, connection_id)` 查询并验证归属。
3. 编排器创建带不可变 `tenant_id` 的任务，且只引用本租户允许的数据集和连接。
4. Worker 以任务租户身份获得对应 Secret 的短期使用许可与受限存储路径。
5. Worker 输出原始数据和结果时，存储层再次验证对象路径、数据集和任务的租户一致性。
6. 任务、Secret 使用和数据访问写入审计事件；错误日志只记录脱敏原因。

### 8.2 Agent 回答数据分析问题

1. API 从认证上下文获取 `tenant_id`，解析用户问题但不信任其中的资源标识。
2. Agent 编排器列出该租户已授权的数据集、任务结果和长期记忆。
3. 检索服务以服务端固定的 `tenant_id` 查询关系库和向量库。
4. 工具调用只能使用被授权的资源引用；模型输出中的越权资源请求被拒绝并记录安全事件。
5. 返回答案前移除内部路径、Secret 和不应显示的元数据；响应及其引用仅归当前租户。

## 9. 测试与验收策略

- **单元测试**：每个资源仓储层的查询都验证包含 `tenant_id`；校验 Secret 脱敏、对象路径生成和跨表归属。
- **集成测试**：创建 A、B 两个租户，覆盖 CRUD、导出、异步任务、重试、缓存、对象下载和记忆检索的交叉访问尝试。
- **RLS 测试**：以运行账号设置 A 的数据库上下文后，直接执行针对 B 的 `SELECT`、`UPDATE`、`DELETE`、`INSERT`，全部应被拒绝或零行影响。
- **Agent 红队测试**：使用提示注入、猜测 ID、伪造工具参数、跨租户相似向量和缓存键碰撞，验证工具与检索层不越权。
- **Secret 扫描**：CI 扫描日志样本、错误上报负载、数据库迁移、代码库与测试夹具，阻止凭据泄露。
- **隔离回归测试**：任何新增实体、队列主题、缓存或索引均需加入 A/B 隔离测试；未覆盖不得上线。
- **删除演练**：验证撤销连接、删除记忆和删除账户能覆盖主数据、对象、向量、缓存和计划中的备份清理。

## 10. 分阶段实施

### 阶段一：隔离基线

- 建立身份认证、租户上下文中间件、数据库 `tenant_id` 约束和 RLS。
- 引入对象存储租户前缀、缓存/队列命名规则与统一审计事件。
- 先实现 API Key/Token 的 Vault 引用存储，不允许明文进入业务库。

### 阶段二：任务与 Agent

- 部署受限 Worker、按租户配额和短期凭据注入。
- 接入记忆策略层、带租户过滤的关系检索和向量检索。
- 建立跨租户自动化测试与 Agent 红队用例。

### 阶段三：治理与强化

- 实现用户自助导出、删除、记忆管理和连接撤销。
- 引入 JIT 管理员访问、密钥轮换、异常检测、备份删除 SLA 和恢复演练。
- 根据风险等级评估独立数据库、每租户密钥、独立执行池等更强隔离选项。

## 11. 上线门槛

上线前必须同时满足：

1. 所有私有实体、对象、缓存、队列和记忆记录都有可验证的 `tenant_id` 归属。
2. 生产应用账号无法绕过数据库 RLS，且跨租户集成测试全绿。
3. Secret 已迁移至密钥服务，日志、遥测和 Agent 上下文的扫描无泄露。
4. Worker 无共享用户状态，网络、文件系统、资源配额和临时凭据均按任务限制。
5. 数据导出、删除、连接撤销、记忆删除和安全审计流程均经过演练。
6. 隐私、保留、支持访问和第三方模型数据处理政策已明确并对用户可见。

