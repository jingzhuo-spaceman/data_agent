{
  "name": "tenant-data-agent-harness",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A secure-by-default, multi-tenant data-agent harness baseline.",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "migrate": "node --env-file-if-exists=.env scripts/migrate.js",
    "seed:dev": "node --env-file-if-exists=.env scripts/seed-development-tenants.js",
    "test": "node --test",
    "test:watch": "node --test --watch"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "pg": "^8.23.0"
  }
}
